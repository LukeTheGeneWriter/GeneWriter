GeneWriter — Session Handoff (v2)

Purpose: context transfer for whoever (human or Claude) picks this up next. Date: 2026-07-24. Supersedes the original Handoff.md (2026-07-23) — that one was design intent from a chat session with the notebooks never actually read; everything below has been read, run, and tested against the real code.

0. Read this first

Unlike v1, this document describes code that exists, runs, and is tested — not a proposal. Where it says something works, it means `pytest` passes on it. Where it says something isn't done, it isn't started. The original 14 notebooks are still in the repo root, unmodified, and are now considered historical/reference material — the live code is `src/genewriter/`.

**Scope note, added 2026-08-19**: this document is a snapshot of the GA/change-vector scoring engine specifically (§1-6 below), last updated 2026-07-24, and does not cover the *baseline-generation* pipeline (`baseline_pipeline.py`, `Baseline_Pipeline.ipynb`, the corpus-wide `Standards/*.json` statistics the GA scoring engine consumes) — that subsystem was rebuilt substantially since this doc was written (chunked/GPU-batched loading, all 5 tests GPU-accelerated, confirmed working live in Colab) and is covered by `HANDOFF_2026-08-19.md` instead. If you're picking up work on how baselines get computed rather than how the GA scores against them, start there, not here.

1. Environment — read before doing anything else

**There is no usable Python on native Windows on this machine.** `python`/`python3` resolve to a Microsoft Store stub, not a real interpreter. All development and testing happens via **WSL (Ubuntu)**. From the repo root:

```
wsl -d Ubuntu -e bash -lc "cd '/mnt/c/Users/lukef/OneDrive/Desktop/GeneWriter' && python3 -m pytest -q"
```

`pyproject.toml` sets `pythonpath = ["src"]`, so no install step is needed. WSL's `python3` had no `pip`; it was bootstrapped via `curl -sS https://bootstrap.pypa.io/get-pip.py | python3 - --user --break-system-packages` (Debian's externally-managed-environment guard blocks a plain `pip install`; no `apt`/`venv`/`ensurepip`/`sudo` available non-interactively). `numpy`/`pytest`/`cupy-cuda12x` and friends are all installed the same way.

**A real CUDA toolchain is set up and verified working**, including a compiled-and-run `.cu` file on the actual GPU (NVIDIA RTX 3050 Laptop, 4GB, Ampere sm_86). GPU driver passthrough (`libcuda.so`) was already present via WSL2's paravirtualization — no setup needed, just `PATH=/usr/lib/wsl/lib:$PATH nvidia-smi` to see it (driver 566.14, CUDA 12.7). The rest (`nvcc`, `gcc`/`g++`, `cupy`) came from pip + user-space Miniforge (conda-forge), no root needed anywhere:

```
curl -sSL -o /tmp/miniforge.sh https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh
bash /tmp/miniforge.sh -b -p $HOME/miniforge3
$HOME/miniforge3/bin/conda install -y -n base -c conda-forge gcc gxx
$HOME/miniforge3/bin/conda install -y -n base -c nvidia -c conda-forge cuda-nvcc=12.9
# plus: pip install --user --break-system-packages cupy-cuda12x nvidia-curand-cu12 nvidia-cuda-nvrtc-cu12
```
Add `$HOME/miniforge3/bin` to PATH for `nvcc`/`gcc`/`g++`. Note pip's own `nvidia-cuda-nvcc-cu12` wheel does *not* ship the real `nvcc` driver binary (only `ptxas` + NVVM) — that's what the conda-forge/nvidia-channel install is for.

Full detail on both of the above (exact failure modes hit, exact fix commands) is saved in this project's Claude memory (`dev_environment.md`) — check there before re-deriving any of it.

2. What's actually in the repo

```
GeneWriter/
├── *.ipynb                    # 14 original notebooks, historical/reference only now
├── Gene_Obj_Samples/           # gitignored: ~6 real NCBI gene JSONs, user-provided, for local dev/testing
├── pyproject.toml
├── src/genewriter/
│   ├── codon_tables.py         # codon<->AA, codon<->index encoding, precomputed per-codon lookup tables
│   ├── classes.py              # canonical dataclasses (from GeneClassesCloud.ipynb)
│   ├── change_vector.py        # the pluggable, vectorized change-vector scoring system
│   ├── gpu_change_vector.py    # population-batched change-vector computation (numpy or cupy backend)
│   ├── ga.py                   # GA primitives: growth, selection, flatten, kill_off, diffing
│   ├── schedule.py             # declarative JSON-able GA schedules (the pipeline-config system)
│   ├── gene_io.py              # load NaturalGene objects from RefGenes-style JSON
│   ├── baseline.py             # compute the 5 *Analysis baseline dataclasses from loaded genes
│   └── standards_io.py         # load precomputed Standards/*.json baselines
└── tests/                      # 138 tests, all passing
```

3. What works, is tested, and is real

- **Full GA pipeline**: seed generation → growth (random mutation + directed evolution) → selection (kill_off / select_survivors / flatten) → repeat, driven either by the simple `ga.run_ga()` convenience function or by a declarative `schedule.py` pipeline (JSON-able step list, matching an ML-training-config shape). Verified end-to-end against real sample gene data.
- **Change-vector scoring**: all 5 original terms (RareCodons, CodonUsage, CodonPairBias, GC, Kmer) ported, bug-fixed, and vectorized with numpy. Pluggable via `@register_term`/`@register_step` decorators — a new objective (uracil minimization, a future RNA-folding signal, etc.) just needs the right function signature, no core code changes.
- **Approximate incremental diffing** (`diff_change_vector`): a child's change vector is estimated from its parent's by recomputing only a local excerpt, instead of a full recompute — exact for 2 of 5 terms (no global dependency), approximate for the other 3 (which fold in a population-wide aggregate). `refresh_change_vectors` forces an exact recompute; wired in automatically before every `kill_off`/`select`/`flatten` step in `schedule.py` (population-decision steps), on a configurable cadence in `run_ga`.
- **Lookahead toggle**: `directed_evolution(..., lookahead=False)` skips the expensive "score every synonymous alternative" step.
- **Population-batched GPU computation** (`gpu_change_vector.py`): all 5 terms, including Kmer (as of this session — see §5), computed for an entire population in one batched pass, on CPU (numpy) or the real GPU (cupy) via the same code. Verified correct against the per-individual implementation and, separately, GPU-vs-CPU-backend agreement on real hardware.
- **GPU batched path is wired into the actual GA loop** (`ga.py`'s `seed_population()`/`refresh_change_vectors(..., xp=...)`/`directed_evolution_batch()`, threaded through `run_ga(..., xp=...)` and `schedule.run_schedule(..., xp=...)` / `ScheduleContext.xp`): every exact, whole-population computation — initial seeding (`run_ga`'s seed list, schedule.py's `"input"` step), every exact refresh (`"kill_off"`/`"select"`/`"flatten"`, and `run_ga`'s periodic `refresh_every`), and (when `lookahead=True`) `"growth"`'s best-alternative scoring — now routes through `batch_calculate_change_vectors()` when `xp=numpy`/`xp=cupy` is passed, instead of a Python loop calling `calculate_change_vector()`/`diff_change_vector()` once per individual. `xp=None` (default) keeps the original per-individual path untouched — this is opt-in, not a behavior change to existing callers. `"growth"`'s new-genotype *storage* (`merge_replicate()`'s per-replicate diffing) is a separate thing and is NOT batched — see §5. Verified end-to-end on real GPU hardware, `xp=numpy` vs `xp=cupy` (not vs `xp=None` — growth's own scoring genuinely changes when xp is set, see §5): identical final populations given the same RNG seed (`tests/test_ga_schedule_gpu_wiring.py`).
- **Data pipeline**: load real NCBI gene JSON → compute baseline statistics → feed the GA. Verified against ~6 real sample genes in `Gene_Obj_Samples/` (not committed; ask the user for a copy, or point at their Colab Drive `RefGenes/NHGeneBodySupp/`).

Every claim above has a corresponding test in `tests/`. Read the module docstrings for the "why" behind each design choice and each historical bug fix — they're written to be self-sufficient; this document is a map, not a replacement.

4. Correctness process worth knowing about

Two real regressions were caught during this session, both by writing a direct comparison against a known-correct reference rather than by code review alone:
- `tests/test_windowed_average.py` — the shared windowing helper, checked against the original pure-Python loop across 550 randomized cases.
- `tests/test_change_vector_reference.py` — all 5 terms, checked against the pre-vectorization implementation (kept verbatim in the test file) across 40 randomized cases. This is what caught a real off-by-one in the RareCodons term that had shipped silently for two turns.
- `tests/test_gpu_change_vector.py` / `test_gpu_change_vector_cuda.py` — the batched implementation, checked against the per-individual one (numpy backend), then the GPU backend checked against the numpy backend on real hardware. This is what caught a GC-term polarity bug.
- `tests/test_ga_schedule_gpu_wiring.py` — the `xp` wiring in `ga.py`/`schedule.py` (see §3), checked by running `run_ga`/`run_schedule` twice with the module RNG reseeded identically (once `xp=None`, once `xp=cupy`) and asserting the final populations are the same genotypes with the same counts, plus a direct `seed_population`/`refresh_change_vectors` numpy-vs-cupy comparison. Same real-GPU-hardware pattern as the cuda file above, same skip-if-no-GPU guard.
- `tests/test_gpu_change_vector.py::test_windowed_average_batched_safe_path_matches_fancy_indexing_reference` — a real CUDA `OutOfMemoryError` on the user's own Colab run (Tesla T4, real 758-residue gene, population=50,000) traced to `gpu_change_vector.windowed_average_batched`'s "safe" fast path: `windows[:, row_starts, :]` used fancy indexing to gather a contiguous range, forcing a full `(P, len(row_starts), 2*half)` materialization instead of a slice-view — the failed allocation (12,532,800,000 bytes) was an exact match for that shape at P=50,000. Fixed by slicing instead (`safe_idx`/`row_starts` are always a single contiguous ascending run — regime_c is itself a contiguous middle band, and the extra bounds are just tighter monotonic conditions on the same index array, which can only narrow an interval, never split it). This helper is shared by *every* batched term (RareCodons/CodonUsage/CodonPairBias/GC/Kmer), so the fix and its regression test (verified against the old fancy-indexing formula directly, not just against the per-individual reference, since two array-backed implementations could share a bug the per-individual comparison wouldn't catch) apply everywhere, not just GC.

If you're extending any of these systems, extend the corresponding reference test rather than trusting a rewrite by inspection — it has a proven catch rate on this exact codebase.

5. What's NOT done — the honest gap list

- **`merge_replicate()`'s per-replicate storage diffing is not batched, and turns out to be the real remaining growth bottleneck.** Found by profiling `directed_evolution_batch()` after Kmer batching (below) landed and growth's *scoring* got fast but growth's *total* time barely moved: on real gene data, batched scoring took 1.45s for ~10,000 candidates, but the `merge_replicate()` loop that stores each accepted replicate's change_vecs (one `diff_change_vector()` Python call each, completely untouched by any of this session's batching) took 12.25s for that same ~10,000 replicates — the actual dominant cost. This is *not* the same code path Kmer batching fixed, and is *not* touched by `xp` at all right now. Batching it the way `seed_population()`/`refresh_change_vectors()` already batch their own storage would be the natural next step — not started. (Also affects `_flatten_round()`'s new-neighbor creation, which has the identical one-diff-call-per-new-genotype shape and wasn't touched either.)
- **Kmer batching: DONE this session** (`gpu_change_vector.batch_kmer_term()`), closing out what used to be the single biggest gap in the batched-computation work. Every k-mer window is re-encoded as a base-4 integer (A/C/G/T → 0..3, `codon_tables.NT_TO_BASE4`/`NT_BASE4_BY_CODON_INDEX`) so the baseline's fold_enrich scores become one flat `(num_buckets, 4**k)` array per k (`_build_kmer_score_table()`), and the substring→score lookup becomes one array-indexed gather across the whole batch instead of a per-window, per-individual Python dict lookup. `batch_calculate_change_vectors()` no longer has *any* per-individual fallback — all 5 terms are genuinely batched. Verified against the per-individual `_kmer_term` (numpy) and numpy-vs-cupy agreement on real hardware (`tests/test_gpu_change_vector.py`/`test_gpu_change_vector_cuda.py`, plus a dedicated multi-k test since the shared test fixture only exercises k=2 — real Standards baselines go up to k=9-10). Table memory is fine at that range (~25MB at k=10) and was flagged as a real limit only well past k=15, not a practical concern today.
- **`"growth"`'s lookahead scoring is batched too** (`ga.directed_evolution_batch()`, wired into `run_ga`/`schedule.py`'s "growth" step whenever `xp` is set and `lookahead=True`) — this was the real per-generation bottleneck originally flagged by the progress instrumentation below (~20-27 individuals/sec, tens of times slower than the batched exact-refresh path, almost certainly the real explanation for multi-hour real-scale runs before this). Growth's new-genotype *storage* is a separate, still-unbatched thing (see the `merge_replicate` bullet above) — only *which alternative gets chosen* is the now-batched part. Net measured speedup for growth end-to-end: a modest ~3x (see §6) — real, but capped by `merge_replicate`'s storage diffing now, not by Kmer anymore.
- **Progress instrumentation added** (`ga.run_ga(progress=, progress_every=)`, `schedule.run_schedule(progress=, progress_every=)`, `gpu_change_vector.batch_calculate_change_vectors(progress_every=)`): diagnostic-only prints (per-generation/per-step timing, plus per-term timing marks within `batch_calculate_change_vectors`, plus every-N-individuals throughput within the remaining per-individual growth/refresh/seeding loops), all default off/silent so existing behavior and tests are unaffected. Added specifically because a long real-scale run gives *zero* feedback otherwise — this is what surfaced the growth bottleneck above (twice — once for lookahead scoring, once for `merge_replicate` after that was fixed); use it before assuming any future slow run is a bug rather than a known cost.
- **RNA folding, SpliceAI, intron insertion**: still entirely unimplemented, exactly as v1 of this handoff flagged. No code exists for any of these anywhere in the repo. Per the original pipeline description (still accurate) these come after the GA/codon-optimization stage.
- **Real-scale (tens of thousands of individuals) has not been run end-to-end.** The fully-batched exact-refresh path was benchmarked up to population=10,000 (see §6), and growth's batching up to population=2,000 — a full schedule run at real scale (18,864 genes, genome-wide Standards) has not been attempted end-to-end by this session, though the user has since run the loader itself against the real data (see the Colab-crash follow-up below) and confirmed load alone takes ~30 minutes, independent of anything covered here.
- **Custom CUDA kernels**: not written. Everything GPU-side so far is CuPy (numpy-compatible array ops, JIT-compiled via NVRTC under the hood) — no hand-written `.cu` kernels in the actual pipeline (only in a standalone verification script proving the toolchain works). The biggest remaining CUDA opportunity is almost certainly RNA folding once it exists (O(n³) DP, the Handoff's own §8-9 already covers this) — bigger than anything left to gain on the already-fast change-vector terms.
- **Flagged-for-later items** (recorded in Claude memory as `future_work_items.md`, not started): n-gram/graph-based population seeding from real codon usage patterns (using the F/T/I/S location tags to restrict to intron-safe interior windows), uracil minimization as a new term, and a precomputed per-codon GC lookup — this last one is now partially done as a side effect of the GPU work (`codon_tables.GC_FLAGS_BY_INDEX`), but not yet wired into the per-individual `_gc_term`.
- **`scripts/colab_stress_test.py`'s `pick_target()` error messages were improved** after a real Colab run (18,864 genes + genome-wide Standards, ~30 min load) crashed with an opaque `ValueError` on an unmatched `GENE_ID`. It now distinguishes "no gene with this ID was loaded at all" from "the gene loaded but every isoform was filtered out by `protein_coding_isoforms()`" (computationally-predicted-only, or empty codon list), and the script's `main()` now documents a load-once/iterate-cheaply cell-splitting pattern for Colab, since re-running `main()` from scratch to try a different `GENE_ID` means re-paying that ~30-minute load.

6. Performance numbers on record (so nobody re-derives these from scratch)

All measured on real gene data (a 59-residue sample gene unless noted; the 398-residue MS4A18 numbers below predate this session), this WSL environment, this RTX 3050.

- `calculate_change_vector()` (one individual, all 5 terms, 398-residue gene): 389ms → 5.7ms after numpy vectorization (~68x).
- `diff_change_vector()` (one child, default margin, 398-residue gene): 41ms → 2.8ms after vectorization (~15x on top of the diffing approximation itself).
- **Full `batch_calculate_change_vectors()` (all 5 terms, now including Kmer) vs. the true per-individual `calculate_change_vector()` loop**, 59-residue gene:
  - population=1,000: per-individual 2.64s → numpy 0.10s (**25.5x**) → GPU 0.08s (**34.9x**)
  - population=10,000: per-individual 27.09s → numpy 1.00s (**27.2x**) → GPU 0.56s (**48.8x**)
  - Superseded numbers from before Kmer batching (4 batchable terms only, Kmer excluded from the comparison entirely): population=1,000 numpy 9.1x/GPU 6.7x; population=10,000 numpy 17.9x/**GPU 24.8x**. Kept for context on how much Kmer batching moved things, not as current guidance — the full-function numbers above are what to use now.
- **Real Colab numbers on a real gene (758 residues, Tesla T4) should be treated as suspect/superseded, not as ground truth for a real-scale gene** — they were measured *before* the `windowed_average_batched` fix above, which every batched term (including GC and Kmer) calls: population=1,000, numpy 27.569s/GPU 23.787s (1.2x); population=10,000, numpy 59.462s/GPU 31.175s (1.9x); population=50,000 crashed with the OOM the fix above addresses. The old fancy-indexing bug alone would have forced a real ~0.25GB (P=1,000) to ~2.5GB (P=10,000) allocation+copy+reduction *per batched-term call* on a gene this length — a real, measurable cost inflating those numbers, not just a hypothetical. **Worth re-running the Colab benchmark now that the fix is in** to get real, trustworthy numbers for a production-scale gene — expect both a faster baseline and (hopefully) a real GPU win to finally show up at population=10,000+ instead of the anemic 1.2x-1.9x above, matching the RTX 3050's 25-48x on a short gene now that the same bug isn't taxing every batched term on a long one.
- **"growth" (`directed_evolution`'s lookahead) was found to be the real per-generation bottleneck at real scale, and is now batchable too** (`ga.directed_evolution_batch()`) — found via the `progress`/`progress_every` instrumentation (see §5) after a real Colab run and a local repro both silently ran for over an hour with no visibility into where the time was going.
  - Before any growth work: lookahead scoring ran at **~20-27 individuals/sec** per-individual — tens of times slower than the batched exact-refresh path.
  - After batching scoring alone (`directed_evolution_batch()`, before Kmer was also batched): population=200, 12.83s → 4.67s; population=2,000, 125.17s → 45.94s (numpy) / 45.33s (cupy) — **~2.7-2.8x**, with GPU giving essentially no win over numpy (1.0x), because Kmer's then-still-per-individual fallback loop now ran on FULL sequences (growth's batched candidates aren't `diff_change_vector`'s bounded ~80-codon excerpts) and dominated regardless of backend.
  - **After Kmer batching also landed, growth's own scoring got fast (1.45s for ~10,000 candidates on real hardware) — but growth's total time barely moved (population=2,000: still ~36-38s, ~3x over the original 114-125s), because a *different*, previously-invisible cost took over: `merge_replicate()`'s per-replicate storage diffing (12.25s for that same ~10,000 replicates), which nothing in this session touches.** GPU still measures ~1.0x vs numpy for growth, for a new reason now — the bottleneck is a Python loop, not batched math, so the array backend is irrelevant to it. This is genuinely a different bug/gap than the one Kmer batching closed, found only by re-profiling after the fix that was expected to help didn't help as much as predicted — see the `merge_replicate` bullet in §5 for the real next step.
  - `lookahead=False` remains a valid alternative mitigation (skips scoring entirely, see `directed_evolution`'s docstring) when growth's current ~3x isn't enough and picking the single best alternative per mutation isn't essential.

7. AA-motif reporting, interior-context codon n-gram seeding, custom targets (2026-07-25) — DONE

Plan approved and fully implemented in the same session — full plan (design rationale, alternatives considered) still at `C:\Users\lukef\.claude\plans\jazzy-puzzling-kite.md` if the "why" behind a decision below isn't obvious from the code's own docstrings. All 202 tests pass (up from 163), including a real end-to-end smoke test tying both new modules together (see below).

**`GeneDataSourcing.ipynb` bug fix — done and verified.** `check_cds_against_protein()`'s per-position amino-acid mismatch check did `continue` instead of rejecting the candidate ATG, so a wrong start was accepted whenever the *last* residue happened to translate correctly by luck, regardless of every earlier mismatch — this is what broke `iso.codons`↔`iso.associatedProtein.aaSeq` alignment in every gene JSON generated before this fix (confirmed universal across every local sample gene). Fixed (`continue` → `break`), edited via a small Python script that mutated only `nb['cells'][3]['source']` and re-serialized with the notebook's original compact JSON formatting (not reformatted/pretty-printed — `git diff --stat` shows exactly 1 line changed). **Verified with a real before/after behavioral test**, not just code review: built a synthetic CDS with a mismatching-but-lucky-last-residue candidate ATG and a genuinely correct one at a different offset — the pre-fix function wrongly picked the bad one, the post-fix function correctly rejects it and finds the real one. `GeneDataSourcing.ipynb`'s own inner-loop `break` was confirmed to only exit the per-position loop, correctly falling through to the outer "try the next candidate ATG" loop.

**Still true and still your responsibility, not done here**: the already-generated gene JSON data (local `Gene_Obj_Samples/` and the real Colab `RefGenes/NHGeneBodySupp/` set) predates this fix and needs regenerating (re-running the notebook pipeline) to actually benefit from it — out of scope for this session, hours of work, entirely your call on timing.

**New: `src/genewriter/aa_motif_index.py`** — amino-acid substring frequency index, fixed k=3..10 (mirrors `KmerAnalysis`'s `kmer_dict` shape), unrestricted by location tag (answers "how common is this motif across the whole proteome", not a codon-choice question). `report_motif_occurrences()` produces exactly `Found {n} occurrences of substring "{s}" in the {label} proteome` — regression-tested verbatim. Reads only `iso.associatedProtein.aaSeq`, never `iso.codons` — no alignment dependency at all, buildable/testable against real data (however imperfect) today.

**New: `src/genewriter/codon_ngram.py`** — codon-choice model conditioned on local amino-acid context (n-gram, descending back-off through `context_orders`, default `(6,5,4,3,2,1)`), restricted to interior (`'I'`-tagged) codon windows by default (`restrict_to_bucket` param, not hardcoded — kept general for the user's stated future intent: letting a custom target mark per-position artificial-intron-insertion points). `sample_codon_ngram_seed()` is a drop-in alternative to `ga.generate_seed()` — always returns a valid codon per residue, backing off to uniform-random when no context meets `min_observations`. **One deliberate deviation from the original plan, for the better**: training derives each position's amino acid by translating `iso.codons` directly (`codon_tables.CODON_TO_AA`), *not* by cross-referencing `iso.associatedProtein.aaSeq` — this means training can't be broken by the codons↔aaSeq alignment bug at all (it only ever needs `iso.codons` to be internally self-consistent, which `locate_codons()` already guarantees regardless of whether `atg_start` was biologically correct). This does **not** make a model trained on pre-fix data scientifically meaningful (if `atg_start` was wrong, `codons` walks a frameshifted, biologically-fake reading frame — self-consistent as codon/context pairs, but not real protein) — the practical failure mode is just gracefully "learns nothing useful, falls back to uniform-random when queried against a real sequence" rather than a crash. Still not a substitute for the real fix + regeneration. `save_codon_ngram_model`/`load_codon_ngram_model` persist a model to JSON (mirrors `standards_io.py`'s pattern) since a real-scale build is exactly the kind of thing worth not recomputing every run.

**Wiring**: `schedule.ScheduleContext.seed_fn` (new, `None` default → `ga.generate_seed`, same opt-in pattern as `xp`), `run_schedule(seed_fn=...)` passthrough. `scripts/colab_stress_test.py`: `TARGET_MODE` (`"gene"`/`"custom"`), `CUSTOM_AA_SEQ`/`CUSTOM_LOCVEC` (bypasses gene lookup entirely, validated via the new `codon_tables.validate_aa_sequence()`), `SEED_STRATEGY` (`"uniform"`/`"ngram"`), `NGRAM_MODEL_PATH`/`NGRAM_CONTEXT_ORDERS`/`NGRAM_MIN_OBSERVATIONS`, new `_build_seed_fn()` helper, `pick_target()`/`run_pipeline()`/`main()` updated accordingly.

**New shared test infrastructure**: `tests/conftest.py`'s `make_synthetic_isoform()`/`make_synthetic_gene()` — the only sanctioned way any test gets codons↔aaSeq alignment (never trust a loaded JSON file for this, even post-fix, until real data is actually regenerated). `tests/test_colab_stress_test.py` is this repo's first test coverage of a script file (loaded via `importlib.util.spec_from_file_location`, not added to `pythonpath`).

**End-to-end smoke test** (synthetic multi-gene fixture, not `Gene_Obj_Samples/`): embedded a repeated 9-residue motif across several genes, confirmed `report_motif_occurrences` produces the exact count and wording; trained a model with a concentrated interior codon context, confirmed sampling reproduced the trained codon in 30/30 seeded trials while every other position stayed valid. Worth knowing for next time: the first version of this smoke test's synthetic data was wrong in an instructive way — a single-residue context ("R" alone) got diluted by unrelated background occurrences of the same amino acid elsewhere in the random test sequences, which used the default (untrained) codon far more often than the deliberately-trained one appeared. Fixed by giving the motif a fixed, repeated multi-residue lead-in so the *specific* n-gram context was unique to the trained signal — a real illustration of why longer context generally beats a single-residue one for this kind of model, not just a testing artifact.

8. CURRENT PRIORITY for next session (2026-07-25 continued): `GeneDataSourcing.ipynb` batch-sourcing robustness

The user is running `downloadGenePackagesAndProcess(NHGenes, r'WorkingFolders')` in Colab against the real ~18,864-gene set (to regenerate data with the §7 notebook fix in effect). Two real issues surfaced during this run; both are now fixed locally, but the second fix has **not yet been verified against a real Colab run** — that's the next thing to confirm once it's pushed and re-run there.

**Fixed and verified**: `iso['genomicLocations']` (cell 3, in `downloadGenePackagesAndProcess`, ~line 509) was accessed unconditionally and raised `KeyError` the first time an isoform lacked it, halting the entire multi-thousand-gene batch. Its siblings (`'cds' in iso`, `'protein' in iso`, `'ensemblTranscript' in iso`) were already defensively guarded a few lines above it — this was just an inconsistency, now fixed the same way: `if 'genomicLocations' in iso: geneLocs = iso['genomicLocations'] else: print(...); geneLocs = []` (skips just that isoform, matching how `'exons' not in gLocSet` is already handled a few lines below). Verified: diff is one clean insertion, both this fix and the §7 `check_cds_against_protein` fix confirmed intact in the file, logic tested standalone for both branches.

**Fixed and verified (2026-07-25, later same day)**: diagnosed below, hit immediately after the `genomicLocations` fix, on gene 2321 (FLT1). Symptom:
```
Error: unable to parse input value as an integer: 'fnamezip'
unzip:  cannot find or open fname, fname.zip or fname.ZIP.
Beginning processing for gene 2321
```
Confirmed **not** a "forgot the `$`" typo — the source (cell 3, ~line 327-336) correctly has `!../../../../datasets download gene gene-id $gene_id --filename $fnamezip --include $argg --no-progressbar` and `!unzip -o $fnamezip -d $fname`, both syntactically valid IPython `!`-magic variable interpolation. But `unzip`'s own error message echoes back exactly what it received as its archive argument — and it received the literal text `"fname"`, not `"2321"` — meaning the substitution genuinely didn't happen for this iteration, for *two different* variables (`$fnamezip` and `$fname`) in the same loop pass. This looks like an intermittent IPython/Colab shell-substitution glitch under a long unattended loop, not a deterministic bug reproducible by re-reading the code — worked fine for prior genes (5080, 138652, etc. all show normal `Archive: <id>.zip` output earlier in this same run).

**The effect is the real danger, more than the glitch itself**: after both the download and unzip calls silently failed, nothing in the loop checks whether they actually succeeded (no return-code check anywhere) — execution falls straight through to reading `2321/ncbi_dataset/data/*` as if nothing went wrong. None of the `if os.path.exists(...)` "Error: 'X' not found" guards fired for gene 2321 (compare to the gene-138652 example earlier in this session, where all 5 fired plainly) — meaning those files *did* already exist on disk, almost certainly stale leftovers from an earlier interrupted run at this same path (`clearDownloadDirectory()` only ever runs after a gene *successfully* completes and saves, so a gene that was mid-flight at the moment of an earlier crash — e.g. the PAX6/`genomicLocations` crash — leaves its download directory behind uncleaned). So the pipeline likely built and saved a `NaturalGene` for FLT1 from **stale, possibly incomplete data**, with no error surfaced beyond the two lines above.

**Fix implemented**: replaced both `!`-magic calls in cell 3's `downloadGenePackagesAndProcess` with explicit `subprocess.run([...])` calls — arguments passed as a real Python list (`['../../../../datasets', 'download', 'gene', 'gene-id', str(gene_id), '--filename', fnamezip, '--include', argg, '--no-progressbar']`, similarly for `['unzip', '-o', fnamezip, '-d', fname]`) instead of through shell-string interpolation. This removes the "did the shell magic actually substitute" ambiguity as a class of bug entirely, and makes the return code checkable: if either call now fails (non-zero returncode), the loop prints the error plus captured stdout/stderr, calls `clearDownloadDirectory('NCBIDownload')`, and `continue`s to the next gene rather than falling through to process whatever happens to already be on disk. `import subprocess` added at the top of cell 3. Same relative path (`../../../../datasets`) and the `sleep(5)` between calls preserved — only the invocation mechanism changed. Edited via the same small script-mutates-`nb['cells'][3]['source']`-and-re-serializes approach as the earlier two fixes in this file (`git diff --stat` shows exactly 1 line changed, since the whole notebook is one compact JSON line). Verified: `ast.parse()` on the cell source (with `%`-magic lines stubbed to `pass`, since they aren't valid standalone Python) confirms no syntax error was introduced; full test suite (203 tests) still passes. **Not yet verified against a real Colab run** — that's the next step (the user is pushing this to GitHub and re-running the Colab batch job against it).

**Still recommended before/while resuming the real run**: manually clear out any stale/partial gene directories left in `NCBIDownload/` from earlier interrupted attempts (the PAX6 crash and this FLT1 incident both likely left debris) — the code fix stops a *silent* fall-through onto stale data, but doesn't retroactively clean up debris from before the fix existed.

**Third real issue, hit on the actual Colab re-run of the `subprocess.run` fix above — fixed and verified**:
```
UnboundLocalError: cannot access local variable 'genomes' where it is not associated with a value
```
at `gSeq = str(genomes[0].seq)`. Real progress, not a regression: the `subprocess.run` fix worked (download+unzip both reported success — this crash happens well after that point, at line ~442), but exposed a preexisting, different bug in the same function. Every one of the 8 downloaded-file checks (`rna.fna`→`rnas`, `5p_utr.fna`→`futrs`, `cds.fna`→`cdss`, `3p_utr.fna`→`tutrs`, `protein.faa`→`proteins`, `gene.fna`→`genomes`, `data_report.jsonl`→`df_final`, `product_report.jsonl`→`df_final1`) only ever prints `"Error: 'X' not found"` on a missing file — none of them actually stop the gene from being processed. Of the 8, `rnas`/`proteins`/`genomes`/`df_final` are read *unconditionally* later in the same function (`genomes[0].seq`, `df_final['symbol'][0]`, etc.) — so any one of those 4 files missing (this time `gene.fna`, for a gene downstream of FLT1) leaves the variable unbound and crashes instead of skipping the gene. (`df_final1` was already incidentally safe — its one unconditional-looking use, `isoforms = df_final1['transcripts'][0]`, is wrapped in a bare `try/except: continue`. `futrs`/`cdss`/`tutrs` are assigned but never read anywhere else in the function — genuinely dead variables, not a crash risk, left alone.)

Fixed by initializing `rnas = proteins = genomes = df_final = None` right before the 8 existence checks, then — right after the last one (`product_report.jsonl`), before `print("Beginning processing for gene "...)` — checking `if rnas is None or proteins is None or genomes is None or df_final is None:` and, if so, printing a diagnostic naming all 4 required files, calling `clearDownloadDirectory('NCBIDownload')`, and `continue`ing to the next gene. Same shape as the two fixes above it: turn a silent fall-through-on-missing-data into an explicit skip-and-report. Verified the same way: `git diff --stat` shows 1 line changed, `ast.parse()` (with `%`-magic stubbed) confirms no syntax error, full test suite (203 tests) still passes. **Still not verified against a real Colab run** — same caveat as the `subprocess.run` fix, both are pending the next real batch attempt.

**Also recommended before resuming the real run**: manually clear out any stale/partial gene directories left in `NCBIDownload/` from earlier interrupted attempts (the PAX6 crash and this FLT1 incident both likely left debris), so a stale directory can't silently mask a fresh failure again the same way.

9. Growth/flatten storage batching, Uracil term, GC-lookup wiring (2026-07-25, same day) — DONE

While the Colab batch-sourcing run from §8 was in progress, this session picked up three items from the honest gap list (§5) and Claude memory's `future_work_items.md` — all independent, bundled into one pass per user confirmation. All 220 tests pass (up from 203), including on real GPU hardware (this RTX 3050, numpy-vs-cupy).

**Growth/flatten's per-replicate storage diffing is now batched — the item §5 flagged as "the natural next step" is done.** Root cause: `directed_evolution_batch()` already computed each scored candidate's *exact* change vector via `batch_calculate_change_vectors()`, but only kept the winning alternative's `new_sol` — the caller (`run_ga`'s growth branch, and its near-duplicate `schedule.py::_step_growth`) then called `merge_replicate()` per replicate, which **recomputed** an approximate result via `diff_change_vector()`, wasting the already-known exact answer. Fixed additively: `directed_evolution_batch()` gained an optional `vecs_out: dict` param (populated with `{tuple(new_sol): vecs}` for every returned replicate — omitting it, every pre-existing call site, is byte-for-byte unchanged behavior). New `merge_replicate_exact(pop_index, sol, vecs)` stores an already-known-exact genotype with the same dedup-by-genotype-key logic `merge_replicate()` has, minus the recompute. New `merge_replicates_batch(pop_index, new_solutions, analysis_objects, locvec, xp, ...)` handles the *other* half of growth — random-mutation replicates, which have no lookahead scoring at all — mirroring `seed_population()`'s dedup-then-batch shape instead of one `diff_change_vector()` call each. Both `run_ga()`'s and `schedule.py`'s growth branches now use these instead of a per-replicate `merge_replicate()` loop, only when `xp` is set (the `xp=None` path is untouched, same opt-in convention as everything else `xp`-gated in this codebase). `_flatten_round()`/`flatten_generation()` got the same treatment (new `xp` param, threaded through both `run_ga`'s `flatten_every` and `schedule.py`'s `"flatten"` step) — simpler case, since flatten's new-neighbor vecs were already exact, not diffed, so batching them is a pure speed win with no accuracy trade-off.

**Real before/after numbers** (real gene data, `Gene_Obj_Samples/`'s longest available protein-coding isoform — ZC3H11C, 805 residues; this WSL environment, this RTX 3050; 200 individuals, 10 replicates each, `xp=numpy`; a fresh measurement, not the historical 59-residue-gene/10,000-replicate numbers in §6, so read as its own data point):
- Random-mutation replicates (2,000): old per-replicate `merge_replicate()` loop 30.86s → new `merge_replicates_batch()` 4.80s (**6.4x**).
- Directed replicates (2,000, vecs already known from scoring): old loop 8.54s → new `merge_replicate_exact()` 0.02s (**361.7x** — this half is now essentially free, since it's a dict-store of an already-computed value instead of a recompute).
- Combined growth-storage total (4,000 replicates): 39.39s → 4.82s (**8.2x**).

**Correctness verified two ways**, per this codebase's "extend/trust the reference test" practice (§4): (1) a numpy-only old-path-vs-new-path equivalence test (`tests/test_ga.py::test_run_ga_growth_batching_matches_reference_merge_path`) runs `run_ga` once through a reference implementation that still does the old one-`merge_replicate()`-call-per-replicate storage (kept inline, not imported, so a future rewrite can't silently drift it), once through the new batched storage, both with the module RNG reseeded identically — asserts identical final genotypes and counts; (2) new numpy-vs-cupy tests in `tests/test_ga_schedule_gpu_wiring.py` (`merge_replicates_batch`, `directed_evolution_batch`'s `vecs_out`, `_flatten_round` with `xp=cupy`) on real GPU hardware.

**A real, separate bug this surfaced, not just a test-writing artifact**: `gpu_change_vector.batch_calculate_change_vectors()` is a hand-written 5-term implementation (`_NATIVELY_BATCHED_TERMS`), *not* generic over `change_vector`'s `@register_term` registry the way `calculate_change_vector()` genuinely is — so the moment a 6th term existed (`Uracil`, below), every batched call (seeding, refresh, growth, flatten — anywhere `xp` is set) was silently **dropping it entirely**, not erroring. Caught by `tests/test_gpu_change_vector.py`'s existing per-individual-vs-batched key-set comparison the moment `Uracil` was registered. This would have been a real, dangerous silent-failure mode in production: configure `weights['Uracil']` nonzero, run at real scale with `xp=cupy` for the (large, necessary) speedup, and the term would just never be scored, with no error anywhere. Fixed with a per-individual fallback loop in `batch_calculate_change_vectors()` for any registered term outside `_NATIVELY_BATCHED_TERMS` — still correct (unlike silently dropping it), just not accelerated unless someone later writes a real batched implementation for it. `register_term()`'s documented "no core code changes needed" promise is now actually true for the batched path too, not just the per-individual one.

**New: Uracil-minimization objective term** (`change_vector._uracil_term`, `@register_term('Uracil')`) — closes `future_work_items.md` item #2. Deliberately *not* a natural-gene-baseline z-score like GC/CodonUsage/Kmer (no new `AnalysisObjects` field) — per that memory note, this is a flat "minimize U content" objective. Score per position: count of U/T bases (in [0,3]) that a synonymous swap could actually remove, gated by the same `CODON_TO_VARIABLE_FLAGS` check `_gc_term` already uses. **Design call flagged in the function's own docstring rather than silently resolved** (per this module's stated policy): the term returns a *positive* "opportunity to remove a U here" count, consistent with every other term scoring "worth mutating" rather than "good."

**Correction (2026-08-04)**: this section originally claimed a *negative* `weights['Uracil']` is what pushes uracil content down. That's backwards, confirmed empirically (see `_uracil_term`'s docstring): `kill_off`/`select_survivors` use the score directly as a death weight via `_finite_nonneg()`, which clamps any negative score to ~0 — a negative weight doesn't invert selection pressure, it neuters it (or inverts it once other positive-weighted terms contribute their own baseline). `directed_evolution`'s lookahead picks whichever synonymous alternative *minimizes* total score, so a negative weight actively prefers the higher-U alternative. A **positive** `weights['Uracil']` — the same "counts normally" convention every other term defaults to — is what actually depletes uracil. Every hardcoded 5-key weights dict in the repo needed a 6th entry (`require_weights()` only requires a weight for whatever keys are actually *in* a given change_vecs dict, so this was never a crash risk for hand-built partial dicts in low-level tests — only for real `weights` dicts paired with real `calculate_change_vector()`/`batch_calculate_change_vectors()` output): `tests/conftest.py`'s shared `weights` fixture and `scripts/colab_stress_test.py`'s `WEIGHTS` both get `Uracil=0.0` (computed, ignored in scoring — the safe default, so no existing test's selection/scoring outcome changed merely from the term existing).

**Also done: `codon_tables.GC_FLAGS_BY_INDEX` wired into the per-individual `_gc_term`** — closes `future_work_items.md` item #3 (previously only used by the GPU-batched `batch_gc_term`). `_gc_term`'s three GC-mean computations now look up the precomputed table (via `CODON_TO_INDEX`) and invert once, mirroring `batch_gc_term`'s already-proven `1.0 - ...` pattern, instead of re-inspecting each codon's characters with three separate list comprehensions. Provably equivalent, not a behavior change — verified via the existing `test_change_vector_reference.py`/`test_gpu_change_vector.py` oracles, both still green.

**Full plan** (design rationale for all three items, alternatives considered) at `C:\Users\lukef\.claude\plans\crispy-exploring-reddy.md` if the "why" isn't obvious from the code's own docstrings.

10. Real-Colab-run finding (2026-07-27): baseline mean/std recomputed from scratch on every batched call — DONE

The user pushed §9 and re-ran the Colab batch benchmark + `run_ga` pipeline against real data (1109 genes, real genome-wide Standards, Tesla T4). Log showed `CodonUsage` costing ~9.4-9.9s and `GC` costing ~7-13s **per batched call regardless of whether the population was 200, 1,000, 10,000, or 50,000** — a flat, population-size-independent cost, the signature of work that scales with the *baseline's* size, not the batch's. Seeding just 200 individuals took 21.62s, almost entirely this overhead — explaining a run that looked "stalled" for several minutes without ever actually being hung.

**Root cause**: `batch_codon_usage_term()`, `batch_gc_term()`, `batch_codon_pair_bias_term()`, and `batch_rare_codon_term()` (`gpu_change_vector.py`) — and their per-individual counterparts in `change_vector.py` — all called `np.mean()`/`np.std()` (or, for CodonPairBias/Kmer, rebuilt a whole lookup table from a dict) directly on **baseline arrays** (`ca.windowdistancesfromoptimal`, `ca.windowscores`, `ca.codonUsageScoreByGene`, `gc.taggedGC1/2/3`, `gc.windows[...]`, `cpb.cpbPerWindow`, `cpb.cpb_lit`, `kmer.kmer_dict[...]`) **on every single call**, never cached. Harmless against the tiny synthetic baselines every local test uses (and even against `rare_codon.usagePerGene`, one value per gene rather than per window) — but real genome-wide Standards baselines are window-level arrays across the whole corpus, large enough that this recompute dominates real-scale cost completely. Pre-existing bug, not introduced by §9's work — just never visible until a real run against real Standards-scale baselines.

**Fix**: `change_vector.cached_stat(obj, key, compute_fn)` / `cached_mean_std(obj, key, values)` — lazily compute and cache directly on the baseline object instance (a private `_stat_cache` dict attribute, not a module-level `id()`-keyed dict, so it's garbage-collected with the object and immune to `id()` reuse). `analysis_objects` is loaded once per run and never mutated afterward by any term function, so this is safe — confirmed by checking every test that does mutate a baseline field in place, and all of them do so *before* that object's first use, on a fresh per-test fixture instance (no test relies on re-mutating an already-used object). Wired into every affected term in both `change_vector.py` (per-individual) and `gpu_change_vector.py` (batched) — **using the same cache keys in both**, so the cache is shared: whichever path runs first for a given `analysis_objects` populates it for both. Turns a per-call cost into a one-time cost for the life of the object, i.e. for a whole GA run.

**Verified two ways**: (1) direct tests of `cached_stat`/`cached_mean_std` (compute-once, scoped per object+key, ignores later mutation of the source list) plus real end-to-end tests that deliberately mutate a baseline field in place *after* a first call and confirm the second call reuses the stale-relative-to-the-mutation cached value rather than recomputing — proof the cache is actually wired into the real term functions, not just that the helper works in isolation (`tests/test_change_vector.py`, `tests/test_gpu_change_vector.py::test_batched_and_per_individual_paths_share_the_baseline_stat_cache` — the latter also proves the per-individual and batched paths genuinely share one cache). (2) Full suite: 226 passing (up from 220). (3) Local before/after benchmark (`Gene_Obj_Samples/`'s longest isoform, 805 residues, population=500, `xp=numpy`): cold-cache first call 0.780s → warm-cache second call 0.578s (**1.3x**) — real, but modest, because local sample data is only 6 genes, nowhere near genome-wide-Standards scale, so the baseline arrays here were never big enough to dominate the way the Colab log showed. **This fix's real target is the Colab-scale pattern in the log above, not the local number** — expect the first batched call in a real run to still pay the baseline-reduction cost once (unavoidable), and every subsequent batched call within that same run (there are many per generation: growth's scoring, `merge_replicates_batch`, the periodic exact refresh, flatten) to no longer show `CodonUsage`/`GC`/`CodonPairBias` costing seconds regardless of population size. **Not yet confirmed against a real Colab re-run** — that's the next step once this is pushed.

11. Population t-SNE visualization (2026-07-27, same day) — DONE

New `src/genewriter/visualize.py`: 2D t-SNE embedding of a population's codon-choice ("genotype") similarity, colored by fitness or any individual change-vector term — cluster on one thing, color by another, so a genotype hotspot the GA converged on and *why* it's doing well (or badly) are both visible at once. Requested by the user specifically to look at neighborhoods of codon streams from a real run.

**Design**: `codon_distance_matrix(pop)` — pairwise Hamming distance (fraction of differing codon positions) via `scipy.spatial.distance.pdist`, using `codon_tables.encode_codons()`. `fitness_color_values(pop, weights, color_by)` — `'fitness'` mode returns the same weighted `score_changevec()` total `kill_off()`/`select_survivors()` already use (not a separate ad-hoc metric); any registered term name (`'GC'`, `'RareCodons'`, `'Uracil'`, etc. — reads `change_vector.registered_terms()`, so a future custom term works with no code change here) returns that term's raw *unweighted* sum, deliberately not multiplied by `weights[term]` so a zero-weighted term (Uracil defaults to 0.0) still shows real signal. `plot_population_tsne(pop, weights, color_by=[...])` computes the embedding **once** and reuses it across one subplot per requested `color_by` mode, so different color modes are directly point-for-point comparable — clustering and coloring are fully decoupled, matching how the user framed the ask.

**Real-scale guard**: exact/precomputed-distance t-SNE is O(P²) memory (a 50,000-individual population would need a ~20GB distance matrix, and that many overlapping points isn't visually useful anyway) — `plot_population_tsne`'s `max_points` (default 2000) uniformly subsamples distinct individuals first; each sampled point's `number` (replicate count) still drives marker size (`size_by_count`, log-scaled) so concentration is visible even after subsampling.

**A real bug caught by actually rendering a figure against real sample gene data (`Gene_Obj_Samples/`), not by the smoke tests alone**: `change_vector._rare_codon_term` legitimately returns `float('inf')` for a rare-codon window composition never observed in the baseline (documented behavior, see that function's own comments) — small/local baselines hit this far more than a real genome-wide Standards baseline would. `'fitness'` inherits the `inf` via `score_changevec()`'s summation. Handing matplotlib's `scatter(c=...)` an array containing `inf` produces `NaN` after color normalization, which it then **silently declines to draw** — no error, just missing points (the `fitness` and `RareCodons` panels rendered completely empty on the first real test, while `GC` rendered correctly with real variation, the tell that this was color-normalization-specific and not a general plotting bug). Fixed with `_sanitize_for_color()`: `+inf`/`-inf` clamp to the finite max/min actually present, `NaN` to the finite mean, all-non-finite falls back to zeros — so an infinite-score individual renders as a visible extreme point instead of vanishing, rather than crashing or silently degrading the whole plot.

**Also**: `load_population_json()` reloads a population saved by `ga.save_gen()` (per-generation JSON in `RUN_GA_OPTIONS['save_dir']`), so a run doesn't need to have just finished, or even be captured in an in-memory variable, to be visualized — the currently-running Colab session's `save_dir="/content/genewriter_runs"` already writes one file per generation. `scripts/colab_stress_test.py`'s documented cell-splitting pattern (bottom of file) got a "cell 3" example calling this against `final_pop` from cell 2. Also fixed a stale comment in that same file's `RUN_GA_OPTIONS` block claiming `merge_replicate`'s storage diffing was "still unbatched" — no longer true as of §9.

**Verified**: 17 new tests (`tests/test_visualize.py`) — distance-matrix correctness (zero diagonal, symmetric, matches manual Hamming fraction), color-value correctness (`'fitness'` matches `score_changevec` exactly, term mode is unweighted, unknown mode raises), the non-finite-value fix (`_sanitize_for_color` unit tests plus an end-to-end test forcing the same all-`inf` `RareCodons` scenario `test_rare_codon_term_never_produces_nan_from_unobserved_window` does, in `test_change_vector.py`, and confirming `fitness_color_values` never leaks a non-finite value), `save_gen`/`load_population_json` round-trip, and figure-structure smoke tests (right subplot count, correct subsampling count reported in the title). Full suite: 243 passing (up from 226). Manually rendered against real local sample gene data (`Gene_Obj_Samples/`'s longest isoform, 805 residues, a real 3-generation `run_ga` population) both before and after the `inf` fix, confirming the bug and the fix visually, not just via assertions.

**Not yet done**: `scikit-learn`/`matplotlib`/`scipy` aren't declared in `pyproject.toml` — matching the existing precedent that `cupy` isn't either (see §1's dev-environment notes); both are preinstalled in Colab by default, so no `pip install` step should be needed there, only for local WSL dev (`pip install --user --break-system-packages scikit-learn matplotlib`). Not confirmed rendering inside an actual Colab notebook cell yet, only locally via the `Agg` backend — Colab's inline plot rendering (`%matplotlib inline` or the default) should just work with `plt.show()`, but hasn't been observed there.

12. New `"directed_growth"` schedule step (2026-07-27, same day) — DONE

Closes `future_work_items.md` item 3, flagged right after the Colab run succeeded. `schedule.py`'s existing `"growth"` step always mixes random-mutation and directed (lookahead) replicates per individual via `directed_fraction` (default 0.5); the user wanted a dedicated step that's *purely* the directed/lookahead path, addressable by name so it can be placed strategically — e.g. right after a `"flatten"` step, so flatten's newly-diversified individuals immediately get pushed toward their locally-best synonymous alternative instead of spending half their replicates on undirected mutation again.

**Implementation**: `_step_growth`'s entire body was factored out into a new shared `_do_growth(pop, ctx, rate, mutation_chance, directed_fraction, lookahead)` helper — `"growth"` and the new `"directed_growth"` step now both call it, differing only in what `directed_fraction` they pass (`params.get("directed_fraction", 0.5)` vs. hardcoded `1.0`). `directed_fraction >= 1.0` skips the random-mutation classification/branch entirely in both the batched and per-individual paths (not just routes zero individuals into it) — both to avoid a wasted `random.random()` draw per individual (keeps `"directed_growth"`'s RNG consumption minimal, not "the same draw as growth, just always landing on directed") and because there's no useful work for the random-mutation machinery to do on an empty list. `"directed_growth"` only takes `rate` and `lookahead` params — no `mutation_chance`/`directed_fraction`, since there's no random branch to configure. Same `xp`-batching behavior as `"growth"` (routes through `ga.directed_evolution_batch()`/`merge_replicate_exact()`/`merge_replicates_batch()` when `ctx.xp` is set and `lookahead=True`), just with 100% of individuals on that path instead of a random subset.

**Verified**: `test_registered_steps_includes_the_builtin_kinds` extended for the new kind; 5 new tests mirroring `"growth"`'s own coverage (dedup-within-step, diffed-storage on the per-individual path, batched-path routing through `directed_evolution_batch()`, and — the one that actually distinguishes it from `"growth"` — a test confirming `replicate_and_mutate_random` is never even called, not just that its output doesn't show up) plus a smoke test running it directly after `"flatten"`, the exact strategic placement it was designed for. Extended `tests/test_ga_schedule_gpu_wiring.py`'s existing numpy-vs-cupy schedule test with a `"directed_growth"` step placed right after `"flatten"` — passes on real GPU hardware (this RTX 3050). Full suite: 248 passing (up from 243).

13. Everything below this line is unchanged from Handoff v1

Still accurate, still not double-checked against code (because the relevant code still doesn't exist): licensing (owner's call, don't touch license files), the CUDA_RNAFold repo open question (language-breakdown mismatch, worth checking `git status`/`.gitignore` there directly), the change-vector design discussion in v1 §5 (Metropolis-Hastings question still unresolved — is the GA's selection step doing proper accept/reject, or is the change vector just a heuristic proposal? worth settling before doing much more GA-tuning work), the fitness-function open concerns in v1 §6 (multi-objective/Pareto vs weighted-sum, MFE-as-wrong-target, pre-/post-splicing sequence conflation, SpliceAI context), the caching design in v1 §7 (not built yet — nothing to cache until folding/SpliceAI exist), the Colab deployment notes in v1 §10, and the LinearDesign benchmark-target idea in v1 §9. Read v1 (in git history, this commit's parent) for the full text of all of these if picking that work up.
